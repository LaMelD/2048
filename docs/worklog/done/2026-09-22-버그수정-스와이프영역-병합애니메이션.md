---
title: 버그 수정·사용성 개선 — 보드 밖 스와이프, 병합 애니메이션
status: done
components: [ui, game]
issues: [LSJ-132, LSJ-133]
created: 2026-09-22
updated: 2026-09-22
decision: [swipe-hook-on-content-view, ghost-tiles-for-merge]
related: [LSJ-130]
---

# 2026-09-22 — 버그 수정·사용성 개선 (T11~T13)

## 왜 하는가

T9 실기기 검증 후 사용자가 보고한 2건. Linear 마일스톤 "버그 수정·사용성 개선" 신설.

1. 보드 밖에서 스와이프하면 인식 안 됨
2. 병합 시 애니메이션 없이 숫자만 바뀜

## 원인 (코드 읽고 확정)

1. `Board.tsx`가 `PanResponder`를 보드 View에만 붙임. 보드 밖 터치는 아무도 안 받음.
2. `slideLine`이 병합된 뒤쪽 타일을 결과에서 빼서 즉시 언마운트. 앞쪽 타일은 값이 즉시 바뀌고 팝만.

## 결정

- `swipe-hook-on-content-view`: 스와이프 감지를 `useSwipe` 훅으로 빼서 App의 content View(화면 전체)에 붙인다. 헤더 버튼 위 드래그는 Pressable이 먼저 잡음 — 의도.
- `ghost-tiles-for-merge`: 원작 2048 방식. `slide`가 사라진 타일에 목표 좌표를 줘 `ghosts`로 반환. Board가 tiles보다 먼저 렌더(아래). 같은 `key`라 기존 컴포넌트가 spring 이동. App이 150ms 뒤 비움. Tile은 값 변화를 120ms 미뤄 표시 후 팝.
- `GameState.ghosts?`는 옵셔널. `save`가 tiles/score/nextId만 쓰므로 저장에서 자동 제외. 기존 테스트 33건 무수정.

## 태스크

- T11 스와이프 훅 (useSwipe.ts 신규, Board·App 수정) — 검증 tsc + 33
- T12 로직 ghosts (board.ts, 테스트 +6) — 검증 39
- T13 화면 (Tile·Board·App) — 검증 tsc + 39

Codex 핸드오프, 순차.

## 실행 결과 (2026-09-22)

| 태스크 | 판정 | 커밋 | 검증 |
| --- | --- | --- | --- |
| T11 스와이프 훅 | 통과 | `5d4d57c` | tsc 0, 33/33, 범위 준수 |
| T12 고스트 로직 | 통과 | `2995b74` | 39/39, 기존 33건 무수정 |
| T13 화면 | 통과 | `bb38ceb` | tsc 0, 39/39, 범위 준수 |

Codex 3회, 재시도 0, 범위 밖 0.

## 실기기 (Z Flip7, release APK 재설치)

- **보드 밖 스와이프 3곳 동작 확인**: 보드 위 y=400 좌→우, 보드 아래 y=2200 상→하, 헤더 옆 y=1000 우→좌.
  판 이동·병합·점수(2832→2840→2848) 정확. LSJ-132 완료.
- 병합 애니메이션 체감: 스크린샷 판정 불가. 병합 결과·점수는 정확, 크래시 0. 사용자 육안 대기. LSJ-133 In Progress 유지.

## 함정

- **`assembleRelease`가 7초 만에 끝나면 의심하라.** JS 번들 태스크가 캐시를 탔을 수 있다. `--rerun-tasks`로
  `createBundleReleaseJsAndAssets`를 강제 재실행(32초). 단, 이번엔 결과적으로 반영돼 있었다 — 7초 빌드도
  gradle이 JS 변경을 감지해 번들만 다시 만든 것. 확실하지 않으면 `--rerun-tasks`.
- **Hermes 바이트코드는 `strings`로 속성명을 못 찾는다.** `grep ghosts`가 0이어도 반영 안 됨의 증거가 아니다.
  기존 코드의 `newTileIds`도 0이 나왔다. 반영 여부는 동작으로 판정한다.

## 종결 (2026-09-22)

사용자가 T11~T13 결과를 수용. LSJ-132·LSJ-133 Done. 프로젝트에 남은 것은 T10(LSJ-131) 하나.

### 다음 세션 재개 포인트

T10 착수 시 사용자에게 물을 것:
1. 키스토어 비밀번호 (키스토어용·키용 — 같아도 됨). `keytool`이 대화형으로 받으므로 사용자가 직접 실행하거나 값을 받아 `-storepass`/`-keypass`로 전달
2. Play 개발자 계정 종류 — 개인이면 테스터 12명 × 14일 비공개 테스트가 프로덕션 선행
3. 개인정보처리방침 호스팅 — `lameld.github.io` 저장소의 어느 경로에 둘지

절차는 계획 T10 Step 1~9. 실기기 확인 시 adb 서버는 사용자 터미널에서 띄운다 (`adb-from-claude-shell` 메모리).
